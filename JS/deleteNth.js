function deleteNth(arr, num) {
  //sort the array
  var sortedArr = arr.slice().sort()
  console.log(sortedArr)
  //check if the next element in the array is the same as the current element
}

deleteNth([1,2,3,4,3], 2)
