package main

import "fmt"

func main() {
  fmt.Println(maxAbs("", ""))
}

func maxAbs(str1 []string, str2 []string) string {
  var res string
  max maxSub int
  if(len(str1) == 0 || len(str2) == 0) {
    res = "Nothing"
  } else {
    for i:=0; i<len(str1); i++ {
      
    }
  }
  return res
}
